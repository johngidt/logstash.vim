logstash.vim highlights configuration files for logstash, a tool for managing events and logs (see http://logstash.net)

install details
Download syntax/logstash.vim to ~/.vim/syntax/ and ftdetect/logstash.vim to ~/.vim/ftdetect.
